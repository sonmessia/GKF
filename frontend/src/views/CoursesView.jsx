import React from 'react';
import { Typography } from '@mui/material';

const CoursesView = () => (
  <div>
    <Typography variant="h4">Courses</Typography>
    <Typography>Courses view - To be implemented</Typography>
  </div>
);

export default CoursesView;
